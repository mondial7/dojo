{
  "name": "dojo-tictactoe",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun build src/client.tsx --outdir=dist && bun run --watch src/server.ts",
    "build": "bun build src/client.tsx --outdir=dist --minify",
    "test": "bun test"
  },
  "dependencies": {
    "@types/bun": "^1.3.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "^5.3.0"
  }
}
